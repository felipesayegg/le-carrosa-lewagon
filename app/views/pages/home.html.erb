<%# <div class="mt-3 mb-3 ml-3 mr-3"> %>

<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://vendedordecarrospro.com.br/wp-content/uploads/2023/07/carro-a-venda.jpg);">
  <div class="container mt-3 mb-3 ml-3 mr-3">
    <h1>Le Carro S.A ! <strong>Melhor</strong> maneira <strong> de </strong>adiquirir e vender seu carro!</h1>
    <p>Facilite sua vida e venha vender e comprar seu carro com a gente.</p>
  </div>
</div>

</div>



<div class="row justify-content-center">
  <div class="col-sm-8 my-3">
    <%= form_with url: root_path, method: :get, class: "d-flex" do %>
      <%= text_field_tag :query,
            params[:query],
            class: "form-control",
            placeholder: "Procure o seu sonho aqui"
        %>
      <%= submit_tag "Search", name: "", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>

<div class="container d-flex justify-content-center">
  <div class="highlights-section sm-col-12 md-col-8">
    <h1> Anuncios em destaque</h1>
    <div class="cards">
      <% @cars.each do |car| %>
        <% if car.user != current_user %>
          <div class="card">
            <div class="img">
              <%= cl_image_tag car.photos.first.key, crop: :fill, class: "img" if car.photos.any? %>
            </div>
            <div class="card-body">
              <h5 class="card-title"><%= car.mod %></h5>
              <p id="card-text-brand" class="card-text"><%= car.brand %></p>
              <div class="text-year-km">
                <p id="card-text-year" class="card-text"><%= car.year %></p>
                <p id="card-text-km" class="card-text"><%= car.km %> <span class="kms-span">kms</span></p>
              </div>
              <p id="card-text-price" class="card-text">R$ <%= car.price %></p>
              <%= link_to "Ver mais", car_path(car), class: "btn btn-primary" %>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
